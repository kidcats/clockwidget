现在有个写界面的想法，就是用两个带旋钮的刻度表，有点类似于小米车上的那个两个大眼睛的东西，然后呢选择好以后
就可以通过一个按钮计算，计算结果就直接显示在最下面。这个界面感觉工作量很大完全不慌。


难搞啊，gpt-4搞不定。还是只能自己来，不过也是好消息，证明了我暂时还不会被取代，
现在有什么说法呢，需要三个参数，分别是valuse,minangle,maxangle
然后画的时候，先根据当前的大小计算centerX和centerY
然后计算比例半径，就是计算高和宽的大小，按照小的那个来画圆（这个我后面肯定要换的，给我换成椭圆谢谢）一个很宽的
然后创建一个黑色的画笔用户画线条
创建另一个黑色的画笔画数字
同时创建一个text的画笔
用最大角度减去最小角度计算总的角度
然后用长度计算总共有多少个刻度
现在的坐标是左上角为坐标原点，所以旋转并移动画布，使其到中心原点
开始遍历每一个
首先获取第一个刻度
然后根据角度计算得到这个刻度的度数
同时每5个一个的方式标记长刻度，根据是否是长刻度来指定刻度的大小
计算中心到刻度线中心的距离
计算长刻度的中心坐标

现在的问题是什么，现在的问题是，角度和位置都不对
那我要做什么，我要做的就很简单，只需要把对的角度和位置计算清楚就好
现在问一下chat,好了，我真是个小天才，直接加一个保存和开始就行了
好了，又有新的问题了，就是为什么后面的只有辅助刻度线没有主刻度线了


解决了，下一问题，字的位置好像不太对，我已经恢复后，是否需要将图像重新旋转

解决了，下个问题，字的位置也对了,但是为什么没有办法自适应变大，这个需要考虑
先看如何搞非常宽的距离

下一个问题，我想要非常宽的距离怎么搞
现在有了新问题，旋转的中心
c